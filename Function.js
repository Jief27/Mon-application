function adresse(ville, pays, quartier) {
    let ville = props.ville;
    let pays = props.pays;
    let quartier = props.quartier;
    return (
        <personne ville={ } pays={ } quartier={ } />)
}

function personne(props) {
    return (props.ville, props.pays, props.quartier)
}